services:
  - type: web
    name: accessiq-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: SECRET_KEY
        value: accessiq-pwc-secret
      - key: JWT_SECRET_KEY
        value: accessiq-jwt-secret